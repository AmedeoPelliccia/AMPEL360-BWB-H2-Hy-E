# Data Directory

This directory stores the SQLite database for PR indexing.

The database file `prs.db` will be created automatically when the server starts.

**Note:** The actual database file is excluded from git via `.gitignore`.
